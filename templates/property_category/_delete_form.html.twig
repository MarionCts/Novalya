<form method="post" action="{{ path('app_property_category_delete', {'id': property_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property_category.id) }}">
    <button class="button button__delete">{{ 'deleteBtn'|trans }}</button>
</form>
