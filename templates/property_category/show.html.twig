{% extends 'base.html.twig' %}

{% block title %}PropertyCategory{% endblock %}

{% block body %}
    <h1>PropertyCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ property_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_property_category_index') }}">back to list</a>

    <a href="{{ path('app_property_category_edit', {'id': property_category.id}) }}">edit</a>

    {{ include('property_category/_delete_form.html.twig') }}
{% endblock %}
